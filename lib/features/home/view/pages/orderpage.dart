import 'package:flutter/material.dart';

class OrdarPage extends StatelessWidget {
  const OrdarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Order Page"),
      ),
    );
  }
}
